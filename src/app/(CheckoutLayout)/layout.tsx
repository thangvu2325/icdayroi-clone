'use client';
import classNames from 'classnames/bind';
import styles from '@/app/(CheckoutLayout)/CheckoutLayout.module.scss';

const cx = classNames.bind(styles);

export default function CheckoutLaytout({ children }: { children: React.ReactNode }) {
  return children;
}
